{
  "name": "visited-ukraine-map",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "test": "concurrently -k -n backend,frontend -c blue,magenta \"npm run test --workspace backend\" \"npm run test --workspace frontend\"",
    "e2e": "npm run e2e --workspace frontend",
    "backup": "npm run backup --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
